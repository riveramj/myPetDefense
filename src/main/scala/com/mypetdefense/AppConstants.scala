package com.mypetdefense

import java.time.ZoneId
import java.util.Locale

object AppConstants {
  val DefaultTimezone: ZoneId = ZoneId.of("America/New_York")
  val DefaultLocale: Locale   = Locale.US
}
